<Window x:Class="MPSim.UI.ChartsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:ScottPlot="clr-namespace:ScottPlot.WPF;assembly=ScottPlot.WPF"
        Title="Визуализация результатов" Height="600" Width="900"
        WindowStartupLocation="CenterOwner"
        
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        FontFamily="{DynamicResource MaterialDesignFont}">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <materialDesign:Card Grid.Row="0" Padding="12" Margin="0,0,0,16"
                             UniformCornerRadius="8"
                             materialDesign:ElevationAssist.Elevation="Dp1">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Тип графика:" 
                           VerticalAlignment="Center" 
                           Margin="0,0,12,0"
                           Style="{StaticResource MaterialDesignCaptionTextBlock}"/>
                <ComboBox x:Name="cbChartType" Width="220" 
                          Style="{StaticResource MaterialDesignComboBox}"
                          SelectionChanged="cbChartType_SelectionChanged">
                    <ComboBoxItem Content="Загрузка ФУ" IsSelected="True"/>
                    <ComboBoxItem Content="Время ожидания"/>
                    <ComboBoxItem Content="Простои ФУ"/>
                    <ComboBoxItem Content="Пропускная способность"/>
                </ComboBox>
            </StackPanel>
        </materialDesign:Card>

        <materialDesign:Card Grid.Row="1" 
                             UniformCornerRadius="8"
                             materialDesign:ElevationAssist.Elevation="Dp1"
                             Padding="8">
            <Grid Background="{DynamicResource MaterialDesignCardBackground}">
                <ScottPlot:WpfPlot x:Name="MainPlot"/>
            </Grid>
        </materialDesign:Card>

        <Grid Grid.Row="2" Margin="0,20,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" x:Name="btnExportCsv" Content="📄 Экспорт CSV" Width="140" Margin="0,0,8,0"
            Style="{StaticResource MaterialDesignRaisedButton}"
            Background="{StaticResource SuccessBrush}"
            Foreground="White"
            Click="btnExportCsv_Click"
            HorizontalAlignment="Left"/>

            <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="btnExport" Content="📥 Экспорт PNG" Width="140" Margin="0,0,8,0"
                Style="{StaticResource MaterialDesignOutlinedButton}"
                Click="btnExport_Click"/>
                <Button x:Name="btnClose" Content="Закрыть" Width="120"
                Style="{StaticResource MaterialDesignRaisedButton}"
                Background="{DynamicResource PrimaryHueMidBrush}"
                Foreground="{DynamicResource PrimaryHueMidForegroundBrush}"
                Click="btnClose_Click"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>